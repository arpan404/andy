{
  "name": "andy",
  "version": "0.1.0",
  "private": true,
  "description": "A TypeScript-first, plugin-native secure AI agent runtime.",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "andy": "bun run --filter @andy/cli dev",
    "build": "bun run --filter './packages/*' build && bun run --filter './apps/*' build",
    "check": "bun run --filter './packages/*' check && bun run --filter './apps/*' check",
    "dev": "bun run --filter @andy/cli dev",
    "test": "bun test packages"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "packageManager": "bun@1.3.7",
  "engines": {
    "node": ">=22"
  }
}
