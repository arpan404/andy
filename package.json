{
  "name": "andy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch index.ts",
    "build": "bun build index.ts --target bun --outdir dist",
    "lint": "biome lint . && bunx oxlint . && bun run lint:limits",
    "lint:limits": "bun run scripts/lint-limits.ts",
    "fmt": "bunx oxfmt --write package.json tsconfig.json tsconfig.base.json biome.json index.ts scripts && biome format --write package.json tsconfig.json tsconfig.base.json biome.json index.ts scripts",
    "fmt:check": "bunx oxfmt --check package.json tsconfig.json tsconfig.base.json biome.json index.ts scripts && biome ci .",
    "typecheck": "tsc --noEmit",
    "test": "bun test --pass-with-no-tests",
    "check": "bun run lint && bun run fmt:check && bun run typecheck && bun run test"
  },
  "dependencies": {
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.13",
    "@types/bun": "^1.3.13",
    "oxlint": "^1.62.0",
    "typescript": "^6.0.3"
  },
  "packageManager": "bun@1.3.7"
}
