{
  "schemaVersion": "1",
  "id": "andy.skills.react-coding",
  "name": "React Coding",
  "version": "0.1.0",
  "description": "Guide React implementation toward component composition, accessibility, and validation.",
  "risk": "high",
  "instructions": "Prefer composable components, accessible controls, stable layout dimensions, and restrained app-like UI. Avoid feature behavior in core; use plugins and APIs.",
  "requiredPlugins": ["andy.project"],
  "requiredCapabilities": [
    "project.read",
    "project.write",
    "project.search",
    "project.run_check"
  ],
  "workflows": [
    {
      "name": "inspect-react",
      "description": "Search for React components and run project validation.",
      "steps": [
        {
          "id": "search-components",
          "toolName": "andy.project.project.search",
          "input": { "query": "function ", "root": "{{input.root}}" },
          "continueOnError": true
        },
        {
          "id": "run-check",
          "toolName": "andy.project.project.run_check",
          "input": {
            "command": "bun",
            "args": ["run", "check"],
            "cwd": "{{input.root}}"
          }
        }
      ]
    }
  ]
}
