{
  "name": "@andy/plugin-memory-markdown",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:binary": "bun build --compile --target=bun --outfile=./dist/plugin ./src/index.ts",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@andy/memory": "workspace:*",
    "@andy/types": "workspace:*",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
