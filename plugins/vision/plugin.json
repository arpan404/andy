{
  "id": "andy.vision",
  "name": "Vision",
  "version": "0.1.0",
  "entry": "./dist/index.js",
  "binaryEntrypoint": "./dist/plugin",
  "executionMode": "subprocess",
  "capabilities": ["camera.read", "screen.capture", "screen.ocr", "screen.describe"],
  "risk": "high",
  "tools": [
    {
      "name": "screen.capture",
      "description": "Capture a screenshot to an approved output path through the current platform adapter.",
      "capabilities": ["screen.capture"],
      "risk": "high",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Screen capture can reveal sensitive data and is policy-gated."
      }
    },
    {
      "name": "screen.describe",
      "description": "Inspect image bytes and prepare an AI SDK image part for direct multimodal LLM input.",
      "capabilities": ["screen.describe"],
      "risk": "medium",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Image description receives explicit image input."
      }
    },
    {
      "name": "screen.ocr",
      "description": "Expose the OCR capability surface; preferred visual reasoning path is direct multimodal LLM image input.",
      "capabilities": ["screen.ocr"],
      "risk": "medium",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "OCR receives explicit image input."
      }
    }
  ]
}
