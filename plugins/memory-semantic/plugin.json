{
  "id": "andy.memory.semantic",
  "name": "Semantic Memory",
  "version": "0.1.0",
  "entry": "./dist/index.js",
  "binaryEntrypoint": "./dist/plugin",
  "executionMode": "subprocess",
  "capabilities": [
    "memory.embed",
    "memory.semantic_query",
    "memory.save",
    "memory.fetch",
    "memory.forget"
  ],
  "risk": "high",
  "tools": [
    {
      "name": "memory.embed",
      "description": "Create a deterministic local embedding vector for semantic-memory indexing.",
      "capabilities": ["memory.embed"],
      "risk": "medium",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Embedding is local deterministic indexing over supplied text."
      }
    },
    {
      "name": "memory.save",
      "description": "Persist an inspectable semantic memory record and local vector index.",
      "capabilities": ["memory.save"],
      "risk": "high",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Semantic memory writes are policy-gated and stored in plugin storage."
      }
    },
    {
      "name": "memory.fetch",
      "description": "Fetch one semantic memory record by id or key.",
      "capabilities": ["memory.fetch"],
      "risk": "medium",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Semantic memory reads are scoped to plugin-owned storage."
      }
    },
    {
      "name": "memory.semantic_query",
      "description": "Query semantic memory records by local vector similarity.",
      "capabilities": ["memory.semantic_query"],
      "risk": "medium",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Semantic query reads plugin-owned memory index."
      }
    },
    {
      "name": "memory.forget",
      "description": "Delete one semantic memory record and its local vector.",
      "capabilities": ["memory.forget"],
      "risk": "high",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Semantic memory deletion is policy-gated and scoped to plugin-owned storage."
      }
    }
  ],
  "memory": {
    "scopes": ["user", "project", "agent", "plugin"],
    "namespaces": ["knowledge", "projects", "documents", "contacts"],
    "retention": "user_controlled",
    "semanticSearch": true,
    "requiresApprovalForScopes": ["user"]
  }
}
