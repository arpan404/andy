{
  "id": "andy.memory.persistent",
  "name": "Persistent Memory",
  "version": "0.1.0",
  "entry": "./dist/index.js",
  "binaryEntrypoint": "./dist/plugin",
  "executionMode": "subprocess",
  "capabilities": [
    "memory.fetch",
    "memory.save",
    "memory.save_fact",
    "memory.query",
    "memory.forget",
    "memory.list"
  ],
  "risk": "high",
  "tools": [
    {
      "name": "memory.save",
      "description": "Persist a structured memory record in plugin-owned storage.",
      "capabilities": ["memory.save"],
      "risk": "high",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Persistent memory writes are policy-gated and stored in plugin storage."
      }
    },
    {
      "name": "memory.save_fact",
      "description": "Persist a fact memory record with fact tagging.",
      "capabilities": ["memory.save_fact", "memory.save"],
      "risk": "high",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Fact memory writes are policy-gated and stored in plugin storage."
      }
    },
    {
      "name": "memory.fetch",
      "description": "Fetch one persistent memory record by id or key.",
      "capabilities": ["memory.fetch"],
      "risk": "medium",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Memory reads are scoped to plugin-owned storage."
      }
    },
    {
      "name": "memory.query",
      "description": "Query persistent memory records by scope, namespace, key, tags, or text.",
      "capabilities": ["memory.query"],
      "risk": "medium",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Memory queries are scoped to plugin-owned storage."
      }
    },
    {
      "name": "memory.list",
      "description": "List persistent memory records.",
      "capabilities": ["memory.list"],
      "risk": "medium",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Memory listing is scoped to plugin-owned storage."
      }
    },
    {
      "name": "memory.forget",
      "description": "Delete one persistent memory record by id.",
      "capabilities": ["memory.forget"],
      "risk": "high",
      "sandbox": {
        "isolation": "sandboxed",
        "compatibleExecutionModes": ["subprocess"],
        "reason": "Memory deletion is policy-gated and scoped to plugin-owned storage."
      }
    }
  ],
  "memory": {
    "scopes": ["user", "project", "session", "agent", "plugin"],
    "namespaces": ["preferences", "facts", "projects", "tasks", "contacts"],
    "retention": "user_controlled",
    "semanticSearch": false,
    "requiresApprovalForScopes": ["user"]
  }
}
