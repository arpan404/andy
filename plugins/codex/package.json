{
  "name": "@andy/plugin-codex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:binary": "bun build --compile --target=bun --outfile=./dist/plugin ./src/index.ts",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@andy/plugin-worker": "workspace:*",
    "@andy/types": "workspace:*",
    "@openai/codex-sdk": "^0.125.0",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^5.9.3"
  }
}
